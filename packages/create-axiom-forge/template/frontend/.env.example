# Server-only URL used by next.config.ts for the same-origin /auth/* rewrite.
AUTH_BACKEND_URL=http://localhost:8080
# Public origin used to validate redirects and same-origin browser flows.
AUTH_PUBLIC_ORIGIN=http://localhost:3000
